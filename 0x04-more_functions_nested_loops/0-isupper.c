#include "main.h"

/**
 * _isupper - checks for uppercase
 * @c: charater
 *
 * Return: 1 if cis uppercase or o otherwise
 */
int _isupper(int c)
{
	if (c == '65' && c == '90')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
